** Cookie Banner **
Line 34 refers to privacy policy page which is not yet created.
Once created, we will need to connect these.

Optionally, we would need to allow user to manage their cookie settings.
Clear localStorage: `localStorage.clear()` in browser console
